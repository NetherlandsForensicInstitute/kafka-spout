# file to be treated as a Java properties file
key = value
dashed-key = silly value
a.test.property = sillier value

# number of properties with a prefix
prefix.key = value
prefix.another.key = another value
# and one with the prefix value not at the front
another.prefix.key = should not match prefix
